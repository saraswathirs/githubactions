ame: action file1
on: [push]
jobs:
  action-job:
    runs-on: ubuntu-latest
    steps: 
      - uses: action/checkout@v2
      - name: say something
        run: echo "i am working github action file"
