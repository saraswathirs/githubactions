name: action file1
on: [push]
jobs:
  action-job:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - name: say something
        run: echo "i am working github action file"
